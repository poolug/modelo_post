// inserta en el index el post creado
$('#posts').prepend('<%= j render(@post, t: @post) %>');

// esconde el formulario
$('#form').empty(500);

// muestra el botón de New Post
$('#create_post').slideDown(100);